spring:
  cloud:
    discovery:
      client:
        simple:
          instances:
            notification-service[0]:
              uri: http://localhost:8084

eureka:
  client:
    fetchRegistry: false
    registerWithEureka: false